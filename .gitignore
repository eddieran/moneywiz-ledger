# Local ledger (contains personal transactions)
data/transactions.csv

# Local overrides (may contain personal account names)
references/config.local.json

# User-specific categories (may contain personal taxonomy/phrases)
references/categories.json
references/category_aliases.json

# Python
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store
